kind: policy
domain: cost
description: Governance policies for Resource Lifecycle
last_updated: '2026-03-27'
rules:
- id: WAF-COST-LIFE-001
  severity: required
  description: Configure auto-shutdown schedules for dev/POC VMs — shut down at 7 PM, no auto-start
  rationale: Dev VMs running 24/7 cost 3x more than VMs with 10-hour daily usage; auto-shutdown eliminates forgotten instances
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    - Microsoft.DevTestLab/schedules
    - Microsoft.OperationalInsights/workspaces
    - Microsoft.KeyVault/vaults
    - Microsoft.RecoveryServices/vaults
    - Microsoft.Resources/resourceGroups
- id: WAF-COST-LIFE-002
  severity: required
  description: Configure storage lifecycle management policies — move to Cool after 30 days, Archive after 90 days, delete
    after 365 days
  rationale: Storage lifecycle policies automatically tier data by age; Cool tier is 50% cheaper than Hot, Archive is 90%
    cheaper
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    - Microsoft.Storage/storageAccounts
    - Microsoft.OperationalInsights/workspaces
    - Microsoft.KeyVault/vaults
    - Microsoft.RecoveryServices/vaults
    - Microsoft.Resources/resourceGroups
- id: WAF-COST-LIFE-003
  severity: required
  description: Set appropriate Log Analytics retention — 30 days for dev/POC, 90 days for production, with archive tier for
    compliance
  rationale: Log Analytics charges per GB ingested and per day retained beyond 31 days; reducing retention from 90 to 30 days
    saves ~65%
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  - monitoring-agent
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    - Microsoft.OperationalInsights/workspaces
    - Microsoft.KeyVault/vaults
    - Microsoft.RecoveryServices/vaults
    - Microsoft.Resources/resourceGroups
- id: WAF-COST-LIFE-004
  severity: required
  description: Configure appropriate soft-delete retention periods — shorter for dev/POC, longer for production
  rationale: Soft-delete protects against accidental deletion but costs storage; longer retention in dev wastes budget
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    - Microsoft.KeyVault/vaults
    - Microsoft.OperationalInsights/workspaces
    - Microsoft.RecoveryServices/vaults
    - Microsoft.Resources/resourceGroups
- id: WAF-COST-LIFE-005
  severity: required
  description: Apply mandatory cost tracking tags to all resources — Environment, CostCenter, Owner, Project
  rationale: Tags enable cost allocation, showback/chargeback, and automated cleanup of orphaned resources
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  - project-manager
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
  - services:
    - Microsoft.Resources/resourceGroups
    - Microsoft.Web/serverfarms
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
  - services:
    - Microsoft.OperationalInsights/workspaces
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
  - services:
    - Microsoft.KeyVault/vaults
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
  - services:
    - Microsoft.RecoveryServices/vaults
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
  - services:
    - Microsoft.Resources/resourceGroups
    terraform_pattern: |
      # === Mandatory Tags (apply to all resources) ===
      locals {
        mandatory_tags = {
          Environment = var.environment         # "dev", "staging", "prod"
          CostCenter  = var.cost_center         # Cost center code for chargeback
          Owner       = var.owner_email         # Owner email for contact
          Project     = var.project_name        # Project name for grouping
          ManagedBy   = "terraform"             # Automation tool
          CreatedDate = formatdate("YYYY-MM-DD", timestamp())
        }
      }

      # === Resource Group with tags ===
      resource "azapi_resource" "resource_group" {
        type      = "Microsoft.Resources/resourceGroups@2024-03-01"
        name      = var.resource_group_name
        location  = var.location
        parent_id = "/subscriptions/${var.subscription_id}"

        tags = local.mandatory_tags

        body = {}
      }

      # === Tag inheritance: all child resources ===
      # Apply local.mandatory_tags to every azapi_resource via top-level tags argument
      resource "azapi_resource" "example_resource" {
        type      = "Microsoft.Web/serverfarms@2023-12-01"
        name      = var.resource_name
        location  = var.location
        parent_id = azapi_resource.resource_group.id

        tags = local.mandatory_tags

        body = {}
      }
    bicep_pattern: |
      // === Mandatory Tags (apply to all resources) ===
      @description('Environment tier: dev, staging, prod')
      param environment string

      @description('Cost center code for chargeback')
      param costCenter string

      @description('Owner email for contact')
      param ownerEmail string

      @description('Project name for grouping')
      param projectName string

      var mandatoryTags = {
        Environment: environment
        CostCenter: costCenter
        Owner: ownerEmail
        Project: projectName
        ManagedBy: 'bicep'
        CreatedDate: utcNow('yyyy-MM-dd')
      }

      // === Apply tags to every resource ===
      resource exampleResource 'Microsoft.Web/serverfarms@2023-12-01' = {
        name: resourceName
        location: location
        tags: mandatoryTags
        // ... resource properties
      }
    prohibitions:
    - NEVER deploy resources without at minimum Environment, CostCenter, Owner, and Project tags
    - NEVER use free-form tag values for Environment — restrict to 'dev', 'staging', 'prod'
    - NEVER omit the ManagedBy tag — it distinguishes IaC-managed from manually-created resources
    - NEVER hardcode tag values — always use variables/parameters for reusability
- id: WAF-COST-LIFE-006
  severity: required
  description: Configure Azure budget alerts with action groups — monthly budget with 50%, 80%, 100%, and 120% thresholds
  rationale: Budget alerts provide early warning before costs exceed expectations; without them, overspend is only discovered
    on invoices
  applies_to:
  - terraform-agent
  - bicep-agent
  - cloud-architect
  - cost-analyst
  companion_resources:
  - type: Microsoft.Insights/actionGroups@2023-01-01
    name: ag-ops
    description: Action group for budget alert notifications — required for budget alerts to trigger email/webhook notifications
  targets:
  - services:
    - Microsoft.Compute/virtualMachines
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
  - services:
    - Microsoft.Consumption/budgets
    - Microsoft.Insights/actionGroups
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
  - services:
    - Microsoft.OperationalInsights/workspaces
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
  - services:
    - Microsoft.KeyVault/vaults
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
  - services:
    - Microsoft.RecoveryServices/vaults
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
  - services:
    - Microsoft.Resources/resourceGroups
    terraform_pattern: |
      # === Budget with Alert Thresholds ===
      resource "azapi_resource" "budget" {
        type      = "Microsoft.Consumption/budgets@2023-11-01"
        name      = "budget-${var.project_name}"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            category  = "Cost"
            amount    = var.monthly_budget       # e.g., 500 for $500/month
            timeGrain = "Monthly"
            timePeriod = {
              startDate = var.budget_start_date   # e.g., "2026-04-01T00:00:00Z"
            }
            filter = {
              tags = {
                name   = "Project"
                values = [var.project_name]
              }
            }
            notifications = {
              "50-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 50
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "80-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 80
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "100-percent" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 100
                thresholdType = "Actual"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
              "120-percent-forecast" = {
                enabled       = true
                operator      = "GreaterThanOrEqualTo"
                threshold     = 120
                thresholdType = "Forecasted"
                contactEmails = var.budget_alert_emails
                contactGroups = [azapi_resource.budget_action_group.id]
              }
            }
          }
        }
      }

      # === Action Group for Budget Alerts ===
      resource "azapi_resource" "budget_action_group" {
        type      = "Microsoft.Insights/actionGroups@2023-01-01"
        name      = "ag-budget-${var.project_name}"
        location  = "global"
        parent_id = azapi_resource.resource_group.id

        body = {
          properties = {
            groupShortName = "budget"
            enabled        = true
            emailReceivers = [
              {
                name                 = "owner"
                emailAddress         = var.owner_email
                useCommonAlertSchema = true
              }
            ]
          }
        }
      }
    bicep_pattern: |
      // === Budget with Alert Thresholds ===
      resource budget 'Microsoft.Consumption/budgets@2023-11-01' = {
        name: 'budget-${projectName}'
        properties: {
          category: 'Cost'
          amount: monthlyBudget
          timeGrain: 'Monthly'
          timePeriod: {
            startDate: budgetStartDate
          }
          filter: {
            tags: {
              name: 'Project'
              values: [projectName]
            }
          }
          notifications: {
            '50-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 50
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '80-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 80
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '100-percent': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 100
              thresholdType: 'Actual'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
            '120-percent-forecast': {
              enabled: true
              operator: 'GreaterThanOrEqualTo'
              threshold: 120
              thresholdType: 'Forecasted'
              contactEmails: budgetAlertEmails
              contactGroups: [budgetActionGroup.id]
            }
          }
        }
      }

      // === Action Group for Budget Alerts ===
      resource budgetActionGroup 'Microsoft.Insights/actionGroups@2023-01-01' = {
        name: 'ag-budget-${projectName}'
        location: 'global'
        properties: {
          groupShortName: 'budget'
          enabled: true
          emailReceivers: [
            {
              name: 'owner'
              emailAddress: ownerEmail
              useCommonAlertSchema: true
            }
          ]
        }
      }
    prohibitions:
    - NEVER deploy a project without a budget resource and alert thresholds
    - NEVER skip the 80% and 100% actual-spend thresholds — they are the primary early warnings
    - NEVER skip the forecasted threshold — it provides advance warning before actual spend occurs
    - NEVER set budget amount without consulting cost-analyst for estimation
    - NEVER use only contactEmails without an action group — action groups support webhooks, Logic Apps, and runbook automation
patterns:
- name: Cost-optimized resource lifecycle
  description: Combine auto-shutdown, lifecycle policies, retention limits, mandatory tags, and budget alerts for comprehensive
    cost governance
anti_patterns:
- description: Do not deploy resources without cost tracking tags
  instead: Apply Environment, CostCenter, Owner, and Project tags to every resource
- description: Do not set unlimited log retention for dev/POC
  instead: Use 30 days for dev/POC; use 90 days with archive tier for production
- description: Do not forget to configure budget alerts
  instead: Create a monthly budget with 50%, 80%, 100% actual and 120% forecasted thresholds
- description: Do not leave dev VMs running 24/7
  instead: Configure auto-shutdown at 7 PM with 30-minute notification
references:
- title: Azure Cost Management best practices
  url: https://learn.microsoft.com/azure/cost-management-billing/costs/cost-mgt-best-practices
- title: Storage lifecycle management
  url: https://learn.microsoft.com/azure/storage/blobs/lifecycle-management-overview
- title: Log Analytics pricing
  url: https://learn.microsoft.com/azure/azure-monitor/logs/cost-logs
- title: Azure budgets
  url: https://learn.microsoft.com/azure/cost-management-billing/costs/tutorial-acm-create-budgets
- title: Azure tagging strategy
  url: https://learn.microsoft.com/azure/cloud-adoption-framework/ready/azure-best-practices/resource-tagging
